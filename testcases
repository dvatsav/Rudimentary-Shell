Internal commands
cd command:

cd
cd ..
cd .
cd ~
cd -
cd ghfjd (corner case: invalid directory)
cd ~
cd /mnt/c/Users/Deepak/Desktop/Deepak/IIIT
cd ../Projects

history command:

history
history -c

pwd command:

pwd

exit command:

exit

External Commands:

ls command:

ls
ls -a
ls -l
ls -m
ls -o
ls fasdfkhj (corner case: invalid directory)
ls ..
ls .
ls ../..
ls -z (invalid option z)

cat command:

cat sdfsdd (corner case: invalid file)
cat ../../Books (corner case: cannot cat a directory)
cat Makefile
cat -E Makefile 
cat -n Makefile
cat -g Makefile (invalid option -g)

date command:

date
date -u
date -R

rm command:

rm tempdir (corner case: cannot delete directory)
rm -r tempdir (works now, can delete directory)
rm -ir tempdir (prompts for deleting)
rm -rf tempdir
rm hello.txt (when a file exists)
rm hello.txt (when hello.txt does not exist)

mkdir command:

mkdir tempdir
mkdir (corner case: requires argument)
mkdir ../hello
